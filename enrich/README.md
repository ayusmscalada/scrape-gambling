# Deprecated: use `app/enrich/`

This directory previously held a duplicate of the enrichment pipeline. The canonical implementation is **`app/enrich/`**, used by `scan_socials.py`. Do not add code here.
